import { Search2filterPipe } from './search2filter.pipe';

describe('Search2filterPipe', () => {
  it('create an instance', () => {
    const pipe = new Search2filterPipe();
    expect(pipe).toBeTruthy();
  });
});
